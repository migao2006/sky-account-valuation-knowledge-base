# v2.4 到 v3 P0 遷移

遷移只讀取原始 ZIP／工作副本，產生全新 v3 目錄。原 ZIP hash 固定為 `6AEEBE997E31B65EF4A7FC70E4905CF4AFAFED01FCA986DD55AB22F63EB98DA3`，遷移前後必須重新驗證。

## 資料處理

- 1,022 筆原始正規化資料遷移為單一 source 與 normalized 契約，並產生 1,022 筆多維 account profile。
- 102 筆市場可比歷程遷移到 curated histories；每筆都有 migration ledger，並與 profile 合併成 102 筆衍生 comparable account。
- 無法遷移的資料不可遺失，必須在報告列出 legacy key、原因和 review 狀態。
- 28 筆正規化資料保留可驗證貼文日期；原可比中可追溯的 5 筆日期已回接 history 與 comparable。`date_verified=true` 沒有日期會被驗證器拒絕；觀察日期不能冒充貼文日期。

## 刪除範圍

不遷移 backtest、calibration、drift、prediction outcome、follow-up、schedule、cycle、Provider、爬蟲、HTTP/API client 及其 fixture、模板、測試和宣告。移除原因記錄於 `reports/migration/removed-features.md`，不把廢棄檔案塞進新資料包。
